# placeholder to fix:
# [ab96cf55-6e72-4a03-a31b-fdb1bab7f647] Missing template, responding with 404: Missing template account/cas, application/cas with {:locale=>[:en], :formats=>[:html], :variants=>[], :handlers=>[:raw, :erb, :html, :builder, :ruby, :rsb]}.

# Searched in:
#   * "/usr/share/webapps/redmine/plugins/redmine_cas/app/views"
#   * "/usr/share/webapps/redmine/app/views"

<%# Minimal fallback so Rails 7 doesn’t raise "Missing template account/cas" %>
<%# If your CAS plugin doesn’t render/redirect explicitly, this will be used %>
OK
